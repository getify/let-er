/* test #6
 * testing how let-er works
 */

let a;

function foo() {
	let;
	let (b)
}

{ let c; 
	let d;
	d++;
	let let { let f;  g++; }
}

function bar() {
	{ let h; 
		let (i) let(j) { letk;  l++; }
	}
}

{ let m; 
	let m;
	m++;
	{ let n; 
		let n;
		n++;
		{ let o; 
			let o;
			o++;
			let let (p)
		}
	}
}
